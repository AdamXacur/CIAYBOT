xac.lat {
    reverse_proxy ciay_frontend:3000
}

www.xac.lat {
    redir https://xac.lat{uri}
}

api.xac.lat {
    reverse_proxy ciay_backend:8000 {
        # Configuración crítica para WebSockets
        header_up Connection {>Connection}
        header_up Upgrade {>Upgrade}
    }
}